# Handoff — finish the *Minority Report* SSRN draft (numeric-provenance-strict)

## RULE 0 — read before doing anything
**Every numeric claim in the paper must be read from the fresh stdout/output of a script you run in this session, under the single consistent methodology below. Never copy a number from this prompt, the prior conversation, the current draft, or memory** — those are orientation only and several were produced by *ad-hoc inline runs under slightly different filters*, so they are NOT trustworthy as-is. Workflow for every number: (1) confirm a saved, versioned script produces it under the consistent methodology; if not, consolidate that computation into a saved script first; (2) run the script; (3) cite from its output; (4) the verifier (Rule: §6) must independently recompute it from source and match. If a number in the current draft cannot be reproduced this way, it is wrong until proven otherwise.

## Project / state
- Repo `~/Documents/git/corrr-pm-obook`; data `~/Documents/data/corrr/390_paper/{build,analysis}`. Branch `claude/oos-robustness-2026-06-01`, PR #1.
- Goal: a revised SSRN draft of `03_paper/main.tex` to send to Prof. Grenadier for feedback. Output file is **`03_paper/2026.06.01_main.tex`** (a copy of `main.tex` being edited; **never edit `main.tex`**). New draft, not a rewrite; ≤ ~4–5 added pages; clearer / less jargon; be forthcoming about corrections.
- WRDS via `~/.Renviron`; connect with `sslmode="require", gssencmode="disable"` + retry. `/opt/anaconda3/bin/python` for python. Full background + verified-number snapshot in `03_paper/2026.06.01_HANDOFF.md` (orientation only — re-run, don't trust).

## Consistent methodology (apply UNIFORMLY to every number)
1. **Beat/miss outcome** (calibration #1, flow #2, wallets #4): original 340 → clean IBES `actual_QTR > line`; new 321 → Dome on-chain resolution (`resolved_beat`). These agree ~95%+ where both exist.
2. **Actual EPS magnitude** (walk-down, implied-EPS, crowd-vs-consensus disagreement): **clean IBES quarterly only** — `pdicity='QTR'`. **yfinance and Compustat are dropped** (do not use them in the paper). Sample = events with IBES (340 full; OOS = IBES-matched ~214).
3. **Consensus** = the market line (slug `eps_target`) — verified ≈ IBES consensus.
4. **Returns / strategy / four-cell / FF3 — all of them**: anchor `t0` on the **actual IBES announcement date (`anndats`)**, not the slug date; **drop look-ahead events** (BMO where the last prediction-market trade is not strictly before `anndats`); winsorize excess returns at 2/98; short strategy return = −(SPY-excess return); thresholds 0.20/0.25/0.30/0.35; horizons 1/5/10 trading days. **This re-anchoring must be applied to the four-cell table and the FF3 regressions too — not only the short-strategy table.** (Several current draft return numbers, incl. the intro "miss-drift −3.16%", were computed on the OLD non-re-anchored windows and must be recomputed.)
5. **Samples**: 340 primary; 321 as an out-of-sample section; pooled 661 as a power check (disjoint, valid to pool).
6. **Framing already decided**: #3 = a robust short-side RETURN that is *marginal/beta-driven* in risk-adjusted terms (NOT factor-neutral alpha). Walk-down magnitude ≈ 0 (the circulated +$1.55 was a quarterly-vs-annual IBES artifact; keep the forthcoming footnote already in the draft). Implied-EPS = the crowd-vs-consensus *disagreement* (real, full-sample) + a dollar-*accuracy* null (crowd ≈ consensus once actuals are quarterly). [The dollar-accuracy piece was sent to Codex in `2026.06.01_implied_eps_dilemma_for_codex.md`; reconcile with Codex's finding before finalizing that subsection.]

## STEP 1 — build ONE consolidated numbers script (do this first)
Create `02_analysis/2026.06.01_paper_numbers.R` that ingests the clean inputs and **emits every number the paper cites, tagged by table/section**, to stdout + a saved `analysis/2026.06.01_paper_numbers.rds`. It must (re)compute, all under the methodology above:
- Primary 340: beat rate; calibration table (5 bins); flow quintile table; Brier/naive/BSS; flow→consensus logit (z) + linear `delta_an~flow` (t); flow tercile table; walk-down regression (α, SE, t, β, R², median surprise); **four-cell returns on RE-ANCHORED returns**; strategy table Panel A (long/short/combined) + Panel B (short 0.20–0.35) on **re-anchored** returns; **FF3 alpha on re-anchored returns** (pooled event-day + decomposition into alpha vs factor contributions + 95% CI).
- OOS 321 (+ post-Feb-18 holdout): #1/#2 (Dome resolution), #3 strategy + FF3 (re-anchored), #4 wallet persistence.
- Pooled 661: strategy + FF3 (this was only ever an *inline* run — it MUST be put in this script).
- Crowd-vs-consensus disagreement (full + OOS): median/% above line, tercile-vs-returns.
- Implied-EPS MAE clean (consensus vs implied, by basis) + waterfall components — pending Codex reconciliation.
- Wallet hit rates (primary clean + OOS), 22 wallets.
Reuse existing scripts as components but make THIS script the single source of truth: `2026.06.01_03g_walkdown_clean.R` (clean QTR actuals → `2026.06.01_walkdown_clean.rds`), `2026.06.01_reanchor_returns.R` (re-anchored returns → `2026.06.01_reanchored_returns.rds`; currently only emits Panel B — extend to Panel A, four-cell, FF3), `2026.06.01_clean_primary_results.R` (NOTE: its four-cell uses OLD windows — fix), `2026.06.01_crowd_vs_consensus.R`, `2026.06.01_oos_tests.R` (re-anchor its returns too).

## STEP 2 — finish the draft edits (cite only STEP-1 output)
Already edited (re-verify against STEP 1): abstract, intro (Brier/flow/returns/contributions), calibration table, flow table, Brier subsection, flow regressions, tercile table, walk-down table + text + footnote.
Remaining:
- Returns section: replace `tab:strategies` with the re-anchored Panel A/B; rewrite Panel A/B prose; add the look-ahead/re-anchoring disclosure (forthcoming, like the walk-down footnote); the four-cell table `tab:four_cell` + drift text recomputed on re-anchored returns.
- FF3 `tab:ff3` + discussion: reframe to marginal/beta-driven; add pooled-661 alpha + the alpha-vs-factor decomposition.
- Linear-return-prediction subsection: re-check the R² and predictors on re-anchored returns.
- Implied-EPS section (`sec:implied`) + the `fig:waterfall` figure: reframe to disagreement + null; the figure currently shows the stale $0.53 decomposition — regenerate with clean numbers or retire it (the in-text reference was already removed).
- Wallets (#4): update hit rate (was 94.1%; clean primary ≈93.5%, OOS ≈92.9% — re-confirm from STEP 1).
- Conclusion: corrected numbers, #3 reframe, add an OOS-validation sentence.
- NEW section "Out-of-Sample Validation" after `sec:micro`: 321 disjoint markets (0 shared ticker-quarters; 115 post-Feb-18 holdout) + pooled 661; report #1/#2/#3/#4 hold; ~2–3 pages.
- Data section (`sec:data`): add the 321 OOS sample, the `pdicity='QTR'` actuals fix, the `anndats` re-anchoring + look-ahead drop, and that the new data uses on-chain resolution as the outcome.

## STEP 3 — verify + compile
- Extend/rewrite `03_paper/2026.06.01_verify_memo.R` into a paper-wide verifier: a record per cited number {location, cited value, scope, recompute() from source under the consistent methodology}; require 100% pass. This enforces RULE 0.
- Compile: `cd 03_paper && pdflatex 2026.06.01_main && bibtex 2026.06.01_main && pdflatex 2026.06.01_main && pdflatex 2026.06.01_main`. Fix references/figure paths. Deliver the PDF.
- Optional: run the `chloe-writes` skill on new prose (then re-confirm no number changed).

## Known-good anchors to sanity-check STEP 1 against (orientation only — reproduce, don't trust)
Clean primary 340: beat rate ~73.7%; Brier 0.130 / BSS +0.33; flow Q1 5.9% / Q5 100%; walk-down α≈−0.01 (t≈−0.27), β≈1.04, R²≈0.98; wallet ~93.5%. Re-anchored short P<0.30 d10 ~+3.45% (t~1.87), P<0.20 ~+4.84% (t~2.04), P<0.35 ~+3.70% (t~2.39). Pooled-661 short P<0.30 d10 ~+3.0% (t~2.3); FF3 alpha marginal (~+0.2%/day, t<1 at P<0.30). If STEP 1 disagrees with these, STEP 1 wins — but investigate the discrepancy.
